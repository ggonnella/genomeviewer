<div id="viewer">
  <%= render :partial => "meta_info" %>
  <div id="viewer_box">
    <%= render :partial => "settings_bar" %>
    <strong><small>Overview of the sequence region:</small></strong>
    <%= render :partial => "orientation_bar" %>
    <br/>&nbsp;
    <%= render :partial => "buttons_bar" %>
    <%= render :partial => "image" %>
  </div>
</div>
<%=javascript_tag <<-end_js
  document.observe("dom:loaded", function() {
    // let the boxover have an higher z-index than the image
    oDv.style.zIndex = parseInt($('image').getStyle("z-index"))+1;
  });
end_js
-%>